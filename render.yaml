services:
  - type: web
    name: keeper-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: SKIP_PREFLIGHT_CHECK
        value: "true"
